# Phase 1: Core Infrastructure Requirements
# These are the minimal packages needed to start modernization
# All packages support Python 3.6+

# Existing requirement
pytest>=6.0.0

# Python 3.6 compatibility
dataclasses>=0.6; python_version < '3.7'
typing_extensions>=3.7.4

# Configuration validation (essential for config refactoring)
pydantic>=1.8.0,<2.0

# Type checking (development)
mypy>=0.910

# Code formatting (development)
black>=21.5b0

# Structured logging (immediate benefit)
structlog>=20.1.0

# Testing enhancements
pytest-cov>=2.10.0
pytest-mock>=3.3.0